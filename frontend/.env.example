# Leave empty in development — Vite proxy forwards /calculate, /export, /integrations to localhost:8000.
# In standalone deployments (not the Docker monolith), set this to the backend URL.
VITE_API_URL=
